import { Product } from "@/interfaces/Types";
import React from "react";

const CategoryContent: React.FC<{ products: Product[] }> = ({ products }: { products: Product[] }) => {
    return (
        <p>
            helo
        </p>
    )
}
export default CategoryContent;